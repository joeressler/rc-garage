# Dependencies and build output stay out of version control
node_modules/
dist/
build/

# Local environment overrides (the committed template is .env.example)
.env

# Framework and editor noise
coverage/
*.log
.DS_Store
.vscode/
.idea/

# Docker and OS artifacts
*.pid

# Local PostgreSQL dumps from scripts/backup-pg.sh / backup-pg.ps1
backups/

.cursor/
