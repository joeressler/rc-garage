import { WorkbenchPlaceholder } from '../components/workbench/WorkbenchPlaceholder';

export function ClipboardPlaceholderView() {
  return (
    <WorkbenchPlaceholder
      drawer="02"
      title="Setup Clipboard"
      milestone="Milestone 11"
      summary="Live FDR sliders, CoG bias bars, and shock dyno cards will mount here. The clipboard editor is intentionally empty until the telemetry store exists."
    />
  );
}
