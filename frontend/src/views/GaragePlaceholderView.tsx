import { WorkbenchPlaceholder } from '../components/workbench/WorkbenchPlaceholder';

export function GaragePlaceholderView() {
  return (
    <WorkbenchPlaceholder
      drawer="01"
      title="Fleet Garage"
      milestone="Milestone 10"
      summary="Chassis bay cards, the fleet rack grid, and vehicle creation land in the garage store milestone. This drawer is wired so the pit-mat shell can navigate without a 404."
    />
  );
}
